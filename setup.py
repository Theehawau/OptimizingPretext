from setuptools import setup, find_packages

setup(
    name='optimizepretext',
    version='1.0',
    description='A module for optimizing pretext tasks.',
    author='Emilio, Hawau and  Karima',
    packages=find_packages(),  # list of all packages
)